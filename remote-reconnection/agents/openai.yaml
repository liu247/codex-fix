interface:
  display_name: "Remote Reconnection"
  short_description: "Fix Codex Desktop remote SSH reconnection loops."
  default_prompt: "Ask for the remote SSH target and proxy URL first, then diagnose and repair Codex Desktop remote reconnection."
